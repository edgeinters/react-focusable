import React from "react"

export interface FocusableProps {
    children?: React.ReactNode
    defaultFocusable: string
}

export const Focusable = (props: FocusableProps) => {

    return (
        <>
            <p>test</p>
        </>
    )

}